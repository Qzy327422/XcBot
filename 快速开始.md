XcBot 快速开始

XcBot 是一款基于 NapCat + OneBot + hyper-bot 的轻量级 QQ 机器人，支持 AI 对话、群/私聊、多模型切换、WebUI 管理及外部插件热加载。

官方 Q 群：1009790417
问题咨询：2472282018

核心特性：
- 多模型 AI 对话（支持Openai格式的模型）
- 群聊和私聊智能响应
- WebUI 可视化管理界面
- 外部插件系统（支持热重载）
- 推荐协议端：NapCatQQ

快速开始：
1. 安装 Python 3.12 或更高版本
2. 执行命令：pip install -r requirements.txt
3. 下载并启动 NapCatQQ，登录机器人 QQ 并开启 OneBot WebSocket 服务
4. 运行main.py，复制控制台输出的WebUI网址，粘贴到浏览器打开
5. 进入Webui，在“连接”选项卡中选择和napcat相同的地址和端口
6. 在 QQ 中发送 /帮助 测试机器人是否回复
7. 在AI 配置选项卡中填写大模型接口


运行要求：
- Python 3.12+
- NapCatQQ 协议端（强烈推荐）
- 一个可用的大模型接口（LLM）


安装依赖：
先升级 pip：
python -m pip install --upgrade pip
然后安装依赖：
pip install -r requirements.txt


接入 NapCatQQ（最重要步骤）：

Windows 用户（推荐一键版）：
1. 下载 NapCat.Shell.Windows.OneKey.zip
2. 解压后运行 NapCatInstaller.exe
3. 启动 NapCatWinBootMain.exe

Linux 用户：
curl -o napcat.sh https://nclatest.znin.net/NapNeko/NapCat-Installer/main/script/install.sh && bash napcat.sh

配置 OneBot WebSocket（关键）：
1. 打开 NapCat WebUI（默认 http://localhost:6099）
2. 前往 网络配置 → 添加 WebSocket 服务器
3. 推荐配置：
   Host：127.0.0.1（同机部署推荐）
   Port：建议使用 5004 或 3333（必须与 config.json 中的端口一致）
4. 确保机器人 QQ 已登录且状态为在线

推荐部署方式：NapCatQQ 和 XcBot 放在同一台机器，使用 127.0.0.1 本地连接，最稳定。



配置机器人：
1. 连接 NapCat：
    连接地址"127.0.0.1",
    连接端口 3333,          // 必须与 NapCat WebSocket 端口一致
    监听地址 "127.0.0.1",
    监听端口 3333

2. LLM 接口配置（AI 核心）：
   示例：
        "base_url": "https://api.deepseek.com/v1",
        "model": "deepseek-chat",
        "keys": ["sk-你的真实Key"]


3. 其他常用配置：
- 人格设定（设置机器人人格）
- 管理用户（填你的 QQ 号，获得管理权限）



WebUI：
默认地址：http://127.0.0.1:7891/
功能包括：查看运行状态、修改配置、管理 LLM 接口、管理插件、查看日志


HTTP 发消息接口（外部程序 / 插件主动推送）：

复用 WebUI 的端口和 Token，不需要额外开端口。启用需要两个条件：
1. WebUI 的 功能配置 → 外部接口 → HTTP 消息发送 开关打开（默认开启）
2. WebUI.access_token 设置一个非空 Token

开关关闭时返回 403 和 FEATURE_DISABLED；Token 为空时返回 503 和 TOKEN_NOT_CONFIGURED。开关改动无需重启，保存后立即生效。

接口：POST http://WebUI地址:端口/api/send
鉴权：请求头 X-WebUI-Token: 你的Token（?token=xxx 仅对背景图与原始日志两个资源路径有效，其余接口一律只认请求头——URL 里的 Token 会进浏览器历史和反代日志）

发私聊纯文本：
curl -X POST http://127.0.0.1:7891/api/send \
  -H "Content-Type: application/json" \
  -H "X-WebUI-Token: 你的Token" \
  -d '{"user_id": 123456789, "text": "你好"}'

发群消息纯文本：
curl -X POST http://127.0.0.1:7891/api/send \
  -H "Content-Type: application/json" \
  -H "X-WebUI-Token: 你的Token" \
  -d '{"group_id": 987654321, "text": "你好"}'

发富消息（文本 + @ + 图片，OneBot 消息段数组）：
curl -X POST http://127.0.0.1:7891/api/send \
  -H "Content-Type: application/json" \
  -H "X-WebUI-Token: 你的Token" \
  -d '{
        "group_id": 987654321,
        "message": [
          {"type": "at",    "data": {"qq": "123456789"}},
          {"type": "text",  "data": {"text": " 请看图"}},
          {"type": "image", "data": {"file": "https://example.com/a.png"}}
        ]
      }'

目标写法（二选一，不能混用）：
- user_id 或 group_id，只能提供一个
- target_type（private / group）+ target_id

消息写法（三选一，不能同时给）：
- text："纯文本"
- message："纯文本" 或 消息段数组
- segments：消息段数组

支持的消息段：
text、image、at、reply、face、record、video、location、contact、json、dice、rps、music
（forward、node、markdown、keyboard、mface 等暂未开放）

限制与安全：
- 请求体最大 4MB；最多 50 个消息段；单个文本段最多 10000 字，文本合计最多 20000 字；json 卡片最多 64KB
- 图片支持 http/https URL 或 base64://（解码后最大 3MB）；语音、视频只支持 http/https URL
- 不允许 file://、本地绝对路径、UNC 路径、data: 等来源，避免外部调用方读取机器人主机上的文件
- at 的 qq 可以填 QQ 号或 all

返回格式：
成功：{"ok": true, "message": "消息已发送", "data": {"target_type": "group", "target_id": "987654321", "segment_count": 3, "message_id": 12345}}
失败：{"ok": false, "code": "错误码", "error": "原因"}

常见状态码：
200 成功；400 参数错误；401 Token 错误；403 消息段或媒体来源被禁止，或功能开关已关闭；413 请求体过大；
429 发送通道繁忙；502 发送失败；503 未配置 Token 或机器人未连接；504 等待回执超时（消息可能已发出，不要无条件重试）

暴露到公网时请用防火墙或反向代理限制来源 IP，并优先使用请求头传 Token。


入群欢迎语自定义：

在 WebUI 的 功能配置 → 入群欢迎 里可以开关这个功能，并直接编辑欢迎语正文。

可用占位符：
{bot_name}       机器人名字
{user_nickname}  新成员昵称
{user_id}        新成员 QQ 号
{group_id}       群号

其他花括号（例如 JSON 片段）会原样保留，不会报错。新成员头像和 @ 由程序固定附加，不需要写在文本里。留空则使用默认文案。


常用命令（前缀 /）：
基础命令：/帮助、/关于、/大头照、/名言
内置插件：/天气 北京、/生图 关键词、/开 QQ号（由 plugins/ 提供，需开启「外部插件加载」开关）
记忆相关：/reset、/压缩状态、/立即压缩
管理命令：/重载插件、/model、/重启、/感知

常见问题：
机器人完全不回复：
- 检查 NapCat 是否在线、QQ 是否登录成功
- OneBot WebSocket 是否已开启
- config.json 中的 host 和 port 是否与 NapCat 一致

AI 不回复：
- llm_endpoints 配置是否正确
- API Key 是否有效且有余额

