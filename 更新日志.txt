v1.4
- 新增 WebUI 调试页，可查看进程 CPU、内存、线程数、日志缓冲、GC 对象、缓存和会话状态。
- 修复 WebUI CPU 占用显示不准确的问题，主页面显示更接近任务管理器的 Bot 进程占用。
- 新增 WebUI hash 路由，支持通过 /#/debug、/#/config、/#/logs 等地址直接打开对应页面。
- 新增 GitHub 备用更新镜像配置，更新检查和下载安装会先直连 GitHub，失败后尝试自定义镜像。
- 优化 WebUI 状态刷新，避免频繁状态请求触发不必要的更新检查。
- 限制昵称缓存大小，降低长时间运行后的内存增长风险。