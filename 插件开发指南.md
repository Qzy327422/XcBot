# XcBot 插件开发指南

## 目录结构

```
plugins/
└── your_plugin/
    ├── setup.py      # 或 main.py，插件入口
    └── metadata.yaml # 插件元数据
```

## 入口文件

入口文件必须定义以下两个内容：

```python
# 触发关键词，使用 "Any" 表示响应所有消息
TRIGGHT_KEYWORD = "关键词"

async def on_message(user_message, user_id, group_id, actions, is_group):
    # user_message: 消息内容
    # user_id: 发送者 QQ 号
    # group_id: 群号（私聊为 None）
    # actions: 发送消息等操作对象
    # is_group: 是否群聊
    await actions.send(
        group_id=group_id,
        message=...
    )
    return True  # 返回 True 表示已处理，阻止后续插件执行
```

`on_message` 的参数按名字注入，只声明你需要的即可，建议末尾带 `**kwargs` 以兼容后续新增字段。

## 触发匹配

命令类插件的匹配是**子串**语义，实际比较 `f"{前缀}{TRIGGHT_KEYWORD}" in f"{前缀}{order}"`。
其中 `order` 是剥掉命令前缀后的文本。

这意味着 `TRIGGHT_KEYWORD = "开"` 时，`/开启服务` 也会命中。如果需要严格前缀匹配，
在 `on_message` 开头自行判断并返回 `False`：

```python
if not order.startswith("开"):
    return False
```

返回 `False` 时主程序会继续走后续插件和 AI 流程。

## 可用的上下文参数

除 `event` / `actions` / `order` / `user_message` / `user_id` / `group_id` / `is_group` 外，
还可以按名字获取：

| 参数 | 说明 |
|---|---|
| `Manager` / `Segments` | Hyper 消息构造 |
| `bot_name` / `bot_name_en` / `reminder` / `ONE_SLOGAN` | 机器人名称与命令前缀 |
| `ADMINS` / `SUPERS` / `ROOT_User` | 管理员列表 |
| `filter_sensitive_content` | 敏感词替换函数，输出文本建议过一遍 |
| `is_feature_enabled` | 读功能开关，如 `is_feature_enabled("weather", True)` |
| `version_name` | 当前主程序版本 |
| `ws_url` | 已拼好的 OneBot WebSocket 地址 |
| `log` | 带插件名前缀的日志函数 |
| `cmc` / `AIbot` / `sys_prompt` | AI 上下文与对话适配器 |

## 帮助菜单

定义 `HELP_MESSAGE` 后，内容会自动追加到 `/帮助` 菜单的「🔌 插件命令」区块：

```python
HELP_MESSAGE = "/天气 [城市] —— 查询天气（插件）"
```

多行会逐行收集，空行自动忽略。

## 日志

通过注入的 `log` 打日志，会自动带上插件名：

```python
async def on_message(order, log=None, **kwargs):
    if log:
        log("开始查询", "HTTP")   # 输出 [HTTP][your_plugin] 开始查询
```

第二个参数是标签，默认 `PLUGIN`。

## 声明主程序版本要求

主程序在执行插件代码前会注入 `XCBOT_VERSION`。在模块顶层校验，不满足就抛异常——
主程序会捕获并把失败原因显示在 WebUI 插件页：

```python
import re

MIN_XCBOT_VERSION = "v2.4"


def _parse_ver(text):
    # 不能直接比较字符串：字典序下 "v2.10" < "v2.4"
    nums = re.findall(r"\d+", str(text or ""))
    return tuple(int(x) for x in nums) if nums else (0,)


current = globals().get("XCBOT_VERSION")
if not current:
    raise RuntimeError(f"无法获取主程序版本，本插件需要 XcBot >= {MIN_XCBOT_VERSION}")
if _parse_ver(current) < _parse_ver(MIN_XCBOT_VERSION):
    raise RuntimeError(f"主程序版本过低：当前 {current}，需要 >= {MIN_XCBOT_VERSION}")
```

## metadata.yaml

```yaml
name: your_plugin
version: 1.0.0
description: 插件描述
author: 你的名字
entry: setup.py
min_xcbot_version: v2.4
```

`metadata.yaml` 仅供插件商店展示，主程序加载时不读取它——版本检查要写在入口文件里。

## 内置插件参考

`plugins/` 下的 `weather`、`image_search`、`check_account` 是从主程序迁移出来的官方插件，
可作为完整示例参考。

## 向商店提交插件

### 方式一：GitHub 网页上传（推荐新手）

1. 打开 [Qzy327422/XcBot-Plugins](https://github.com/Qzy327422/XcBot-Plugins)，点击右上角 **Fork**
2. 进入你 Fork 后的仓库，点击 `plugins/` 文件夹
3. 点击右上角 **Add file → Upload files**，把你的插件文件夹打包成 zip 后上传，或者用 **Create new file** 逐个创建文件
4. 填写提交信息，点击 **Commit changes**
5. 回到仓库主页，点击 **Contribute → Open pull request**，描述插件功能后提交

### 方式二：git 命令行

```bash
git clone https://github.com/你的用户名/XcBot-Plugins.git
cd XcBot-Plugins/plugins
mkdir your_plugin && cd your_plugin
# 放入 setup.py 和 metadata.yaml
cd ../..
git add .
git commit -m "Add your_plugin"
git push
# 然后在 GitHub 网页提交 Pull Request
```

## 本地安装插件

在 WebUI 插件商店页面右下角点击 **⇧** 上传按钮，选择打包好的 `.zip` 文件即可。

zip 文件结构：

```
your_plugin.zip
├── your_plugin/      # 顶层目录（可有可无，会自动剥离）
│   ├── setup.py
│   └── metadata.yaml
```

安装后如插件未自动生效，发送 `/重载插件` 命令。
